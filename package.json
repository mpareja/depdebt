{
  "name": "dependency-debt",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "private": false,
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "clean": "rimraf coverage",
    "cov": "opener coverage/index.html",
    "jest": "",
    "lint": "standard -v",
    "prepare": "simple-git-hooks",
    "test": "npm run clean && npm run lint && npm run tests -- --experimental-test-coverage",
    "test:interactive": "node --test --require ./setup-tests.js interactive/*.js",
    "tests": "node --test --require ./setup-tests.js"
  },
  "standard": {
    "env": [
      "jasmine",
      "node"
    ]
  },
  "simple-git-hooks": {
    "pre-commit": "npm test"
  },
  "author": "Mario Pareja",
  "license": "MIT",
  "devDependencies": {
    "@jest/expect": "^29.5.0",
    "opener": "^1.5.2",
    "simple-git-hooks": "^2.8.1",
    "standard": "^17.1.0"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/mpareja/dependency-debt.git"
  },
  "bugs": {
    "url": "https://github.com/mpareja/dependency-debt/issues"
  },
  "homepage": "https://github.com/mpareja/dependency-debt#readme",
  "dependencies": {
    "npm-pick-manifest": "^8.0.1",
    "pacote": "^15.2.0"
  }
}
